import React from "react";
import Book from "../Book";

const Education = () => {
  return (
    <section id="education" className="bg-pink flex justify-center items-center p-4 h-[30rem] bg-gray">
      <Book />
      <div>
        {/* Miami Unversity with Course Projects, thesis ? */}
      </div>
      {/* Nepal to USA animation */}
      <div>
        {/* Pulchowk Campus with Electronics and Computer animated background, something like pcb path animation, coding animation
      like in movies */}
      </div>
    </section>
  );
};

export default Education;
